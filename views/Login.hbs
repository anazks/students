<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Teacher Login</title>
  <style>
    /* General Styles */
    body {
      margin: 0;
      padding: 0;
      font-family: 'Arial', sans-serif;
      background: linear-gradient(to bottom, #4a90e2, #1c3b70);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      color: #fff;
    }

    /* Container */
    .login-container {
      display: flex;
      justify-content: center;
      align-items: center;
      width: 100%;
      height: 100%;
    }

    /* Login Card */
    .login-card {
      position: relative; /* For positioning child elements */
      background: rgba(255, 255, 255, 0.9);
      border-radius: 10px;
      padding: 30px 40px;
      width: 400px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
      color: #333;
      text-align: center;
    }

    .login-title {
      font-size: 24px;
      margin-bottom: 10px;
    }

    .subtitle {
      font-size: 14px;
      margin-bottom: 20px;
      color: #666;
    }

    /* Form */
    .form-group {
      margin-bottom: 20px;
      text-align: left;
    }

    label {
      font-size: 14px;
      margin-bottom: 5px;
      display: block;
    }

    input {
      width: 100%;
      padding: 10px;
      font-size: 14px;
      border: 1px solid #ccc;
      border-radius: 5px;
      margin-top: 5px;
    }

    input:focus {
      outline: none;
      border-color: #4a90e2;
      box-shadow: 0 0 4px rgba(74, 144, 226, 0.5);
    }

    /* Buttons */
    .login-button {
      width: 100%;
      padding: 10px 15px;
      background-color: #4a90e2;
      border: none;
      color: #fff;
      font-size: 16px;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    .login-button:hover {
      background-color: #3b78c2;
    }

    /* Top-right Button */
    .student-login-button {
      position: absolute; /* Positioned relative to the card */
      top: 10px;
      right: 10px;
      padding: 8px 12px;
      background-color: #f76c6c;
      color: #fff;
      font-size: 14px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s ease;
      display: flex;
      align-items: center;
    }

    .student-login-button:hover {
      background-color: #e05757;
    }

    .student-login-button i {
      margin-right: 5px;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <div class="login-card">
      <button class="student-login-button" onclick="redirectToStudentLogin()">
        <i class="fas fa-user-graduate"></i> Student Login
      </button>
      <h1 class="login-title">Teacher Login</h1>
      <p class="subtitle">Welcome to the Lab Log System</p>
      <form id="loginForm" action="/login" method="POST">
        <div class="form-group">
          <label for="email">Email Address</label>
          <input type="text" id="email" name="username" placeholder="Enter your email" required>
        </div>
        <div class="form-group">
          <label for="password">Password</label>
          <input type="password" id="password" name="password" placeholder="Enter your password" required>
        </div>
        <button type="submit" class="login-button">Login</button>
      </form>
    </div>
  </div>
  <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
  <script>
    function redirectToStudentLogin() {
      window.location.href = '/student/login';
    }
  </script>
</body>
</html>
