<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>View Student</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(to bottom, #f8f9fa, #e9ecef);
      font-family: 'Roboto', sans-serif;
    }

    .breadcrumb {
      background-color: transparent;
      font-size: 1.1rem;
      padding-left: 0;
    }

    .breadcrumb-item a {
      color: #010101;
      text-decoration: none;
      transition: color 0.3s ease;
    }

    .breadcrumb-item a:hover {
      color: #218838;
    }

    .view-user {
      background-color: #ffffff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
      margin-bottom: 30px;
    }

    .view-user h3 {
      font-family: 'Roboto', sans-serif;
      color: #343a40;
      font-weight: 600;
    }

    .form-control {
      border: none;
      background-color: #f8f9fa;
      font-size: 16px;
      font-weight: bold;
      color: #495057;
      pointer-events: none; /* Make fields uneditable */
    }

    .form-group {
      margin-bottom: 20px;
    }

    @media (max-width: 768px) {
      .view-user {
        padding: 15px;
      }

      .view-user h3 {
        font-size: 1.5rem;
      }
    }
  </style>
</head>

<body>
  <!-- Breadcrumb -->
  <nav aria-label="breadcrumb" class="mt-4 ms-4">
    <ol class="breadcrumb bg-transparent p-0">
      <li class="breadcrumb-item">
        <a href="/" class="text-light btn btn-success">Home</a>
      </li>
      <li class="breadcrumb-item active" aria-current="page">View Student</li>
    </ol>
  </nav>

  <!-- View Student Section -->
  <div class="container mt-5">
    {{#each rows}}
    <div class="view-user">
      <div class="row mb-4">
        <div class="col text-center">
          <h3 class="display-6">{{this.name}}</h3>
        </div>
      </div>
      <form>
        <div class="form-group row">
          <label class="col-sm-4 col-form-label text-end">Student ID:</label>
          <div class="col-sm-8">
            <input type="text" class="form-control" value="{{this.id}}" readonly>
          </div>
        </div>

        <div class="form-group row">
          <label class="col-sm-4 col-form-label text-end">Name:</label>
          <div class="col-sm-8">
            <input type="text" class="form-control" value="{{this.name}}" readonly>
          </div>
        </div>

        <div class="form-group row">
          <label class="col-sm-4 col-form-label text-end">Batch:</label>
          <div class="col-sm-8">
            <input type="text" class="form-control" value="{{this.batch}}" readonly>
          </div>
        </div>

        <div class="form-group row">
          <label class="col-sm-4 col-form-label text-end">Gender:</label>
          <div class="col-sm-8">
            <input type="text" class="form-control" value="{{this.gender}}" readonly>
          </div>
        </div>

        <div class="form-group row">
          <label class="col-sm-4 col-form-label text-end">Department:</label>
          <div class="col-sm-8">
            <input type="text" class="form-control" value="{{this.department}}" readonly>
          </div>
        </div>

        <div class="form-group row">
          <label class="col-sm-4 col-form-label text-end">Phone:</label>
          <div class="col-sm-8">
            <input type="text" class="form-control" value="{{this.phone}}" readonly>
          </div>
        </div>

        <div class="form-group row">
          <label class="col-sm-4 col-form-label text-end">Email:</label>
          <div class="col-sm-8">
            <input type="email" class="form-control" value="{{this.email}}" readonly>
          </div>
        </div>
      </form>
    </div>
    {{/each}}
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
